// File generated from our OpenAPI spec by Stainless. See CONTRIBUTING.md for details.
import { AbstractPage } from "./core.mjs";
export class BlueprintsCursorIDPage extends AbstractPage {
    constructor(client, response, body, options) {
        super(client, response, body, options);
        this.blueprints = body.blueprints || [];
        this.has_more = body.has_more || false;
        this.total_count = body.total_count || 0;
    }
    getPaginatedItems() {
        return this.blueprints ?? [];
    }
    // @deprecated Please use `nextPageInfo()` instead
    nextPageParams() {
        const info = this.nextPageInfo();
        if (!info)
            return null;
        if ('params' in info)
            return info.params;
        const params = Object.fromEntries(info.url.searchParams);
        if (!Object.keys(params).length)
            return null;
        return params;
    }
    nextPageInfo() {
        const blueprints = this.getPaginatedItems();
        if (!blueprints.length) {
            return null;
        }
        const id = blueprints[blueprints.length - 1]?.id;
        if (!id) {
            return null;
        }
        return { params: { starting_after: id } };
    }
}
export class DevboxesCursorIDPage extends AbstractPage {
    constructor(client, response, body, options) {
        super(client, response, body, options);
        this.devboxes = body.devboxes || [];
        this.has_more = body.has_more || false;
        this.total_count = body.total_count || 0;
    }
    getPaginatedItems() {
        return this.devboxes ?? [];
    }
    // @deprecated Please use `nextPageInfo()` instead
    nextPageParams() {
        const info = this.nextPageInfo();
        if (!info)
            return null;
        if ('params' in info)
            return info.params;
        const params = Object.fromEntries(info.url.searchParams);
        if (!Object.keys(params).length)
            return null;
        return params;
    }
    nextPageInfo() {
        const devboxes = this.getPaginatedItems();
        if (!devboxes.length) {
            return null;
        }
        const id = devboxes[devboxes.length - 1]?.id;
        if (!id) {
            return null;
        }
        return { params: { starting_after: id } };
    }
}
export class RepositoriesCursorIDPage extends AbstractPage {
    constructor(client, response, body, options) {
        super(client, response, body, options);
        this.repositories = body.repositories || [];
        this.has_more = body.has_more || false;
        this.total_count = body.total_count || 0;
    }
    getPaginatedItems() {
        return this.repositories ?? [];
    }
    // @deprecated Please use `nextPageInfo()` instead
    nextPageParams() {
        const info = this.nextPageInfo();
        if (!info)
            return null;
        if ('params' in info)
            return info.params;
        const params = Object.fromEntries(info.url.searchParams);
        if (!Object.keys(params).length)
            return null;
        return params;
    }
    nextPageInfo() {
        const repositories = this.getPaginatedItems();
        if (!repositories.length) {
            return null;
        }
        const id = repositories[repositories.length - 1]?.id;
        if (!id) {
            return null;
        }
        return { params: { starting_after: id } };
    }
}
export class DiskSnapshotsCursorIDPage extends AbstractPage {
    constructor(client, response, body, options) {
        super(client, response, body, options);
        this.snapshots = body.snapshots || [];
        this.has_more = body.has_more || false;
        this.total_count = body.total_count || 0;
    }
    getPaginatedItems() {
        return this.snapshots ?? [];
    }
    // @deprecated Please use `nextPageInfo()` instead
    nextPageParams() {
        const info = this.nextPageInfo();
        if (!info)
            return null;
        if ('params' in info)
            return info.params;
        const params = Object.fromEntries(info.url.searchParams);
        if (!Object.keys(params).length)
            return null;
        return params;
    }
    nextPageInfo() {
        const snapshots = this.getPaginatedItems();
        if (!snapshots.length) {
            return null;
        }
        const id = snapshots[snapshots.length - 1]?.id;
        if (!id) {
            return null;
        }
        return { params: { starting_after: id } };
    }
}
//# sourceMappingURL=pagination.mjs.map