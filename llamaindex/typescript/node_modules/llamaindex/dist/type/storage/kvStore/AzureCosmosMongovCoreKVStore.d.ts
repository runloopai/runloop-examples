import { BaseKVStore } from "@llamaindex/core/storage/kv-store";
import { MongoClient } from "mongodb";
export interface VcoreConnectionStringOptions extends AzureCosmosVCoreKVStoreConfig {
    connectionString?: string;
}
export interface AzureCosmosVCoreKVStoreConfig {
    mongoClient?: MongoClient;
    dbName?: string;
    collectionName?: string;
}
export declare class AzureCosmosVCoreKVStore extends BaseKVStore {
    private mongoClient;
    private dbName;
    private collectionName;
    private collection?;
    /**
     * Create a new AzureCosmosDBNoSQLVectorStore instance.
     */
    constructor({ mongoClient, dbName, collectionName, }: AzureCosmosVCoreKVStoreConfig);
    client(): MongoClient;
    private ensureCollection;
    put(key: string, val: Record<string, any>): Promise<void>;
    get(key: string): Promise<Record<string, any> | null>;
    getAll(): Promise<Record<string, Record<string, any>>>;
    delete(key: string): Promise<boolean>;
}
